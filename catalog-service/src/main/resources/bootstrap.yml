spring:
  application:
    name: catalog-service
  cloud:
    config:
      uri: http://${CONFIG_SERVER_HOST}:${CONFIG_SERVER_PORT}

server:
  port: 8081

logging:
  level:
    org.art.web:DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"

